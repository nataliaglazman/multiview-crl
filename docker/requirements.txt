# PyTorch — pinned to match the cu121 build installed by the SLURM env setup.
# Unpinned ranges let this sync pull a wheel built against a different CUDA,
# which breaks unpredictably across the mixed A100/H200/L40S nodes.
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# TensorBoard
tensorboard>=2.13.0

# Medical Imaging
monai>=1.3.0
nibabel>=5.0.0

# Data Science
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Vision/Perception
lpips>=0.1.4

# NLP (for text tokenization in datasets.py)
nltk>=3.8.0

# Utilities
pyyaml>=6.0
typing_extensions>=4.0.0
six>=1.16.0
